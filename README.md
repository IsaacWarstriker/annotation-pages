# Annotation Division — Capacity Pager

Static, single-file site: capacity model (page 1) + notes & definitions (page 2).

- **Edit**: replace `index.html`, commit, push to `main` — GitHub Pages rebuilds in ~1 minute.
- No build step, no dependencies, no external assets.
